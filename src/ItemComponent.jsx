let ItemComponent = React.createClass({
  render: function(){
     return (
       <li>
         this.markComplete(i)}
         className={item.done ? 'completed' : ''}
         key={i}>
         {item.label}
       </li>
     )
  }
}
